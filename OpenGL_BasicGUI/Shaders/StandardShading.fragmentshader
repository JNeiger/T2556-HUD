#version 330 core

in vec3 Position_worldspace;

// Ouput data
out vec3 color;

void main(){
	float r = Position_worldspace.x+1;
	float g = Position_worldspace.y+1;
	float b = Position_worldspace.z+1;

	color = vec3(1,0,0); //vec3( .62,.62,.62 );//
}